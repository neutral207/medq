Place database migration scripts or SQL files here.

Run order:
1. schema.sql  → creates all tables and indexes
2. seed.sql    → optional demo data

Connect using pgAdmin or psql with the DATABASE_URL from backend .env.
